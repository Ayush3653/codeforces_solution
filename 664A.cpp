#include <bits/stdc++.h>
using namespace std;
int32_t main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(NULL);
    string a,b;
    cin>>a>>b;
    if(a==b){
        cout<<a;
    }
    else{
        cout<<"1";
    }
    return 0;
}